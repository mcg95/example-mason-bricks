name: {{app_name.snakeCase()}}
description: A Flutter app generated by the app_skeleton Mason brick.
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
{{#dependencies}}
  {{.}}: any
{{/dependencies}}

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
