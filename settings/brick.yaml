name: settings
description: Generates a settings feature with a ListView containing Profile and Logout options.
version: 1.0.0

environment:
  mason: ">=0.1.0 <1.0.0"

vars:
  include_profile:
    type: boolean
    description: Include Profile option in settings
    default: true
    prompt: Include Profile option?
